<Window x:Class="WarehouseApp.Views.WarehouseDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Детали помещения" Height="408" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Информация о помещении" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

        <GroupBox Grid.Row="1" Header="Название" Margin="0,5">
            <TextBlock x:Name="lblName" Margin="5"/>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Адрес" Margin="0,5">
            <TextBlock x:Name="lblAddress" Margin="5"/>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Особые условия" Margin="0,5">
            <TextBlock x:Name="lblConditions" Margin="5" TextWrapping="Wrap"/>
        </GroupBox>

        <GroupBox Grid.Row="4" Header="Дата добавления" Margin="0,5">
            <TextBlock x:Name="lblDate" Margin="5"/>
        </GroupBox>

        <Button Grid.Row="5" x:Name="btnClose" Content="Закрыть" Width="100" Height="35" 
                HorizontalAlignment="Center" Margin="0,20" Click="BtnClose_Click"/>
    </Grid>
</Window>